[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --verbose --strict-markers --cov=app --cov-report=xml --cov-report=term-missing
markers =
    unit: Quick unit tests
    integration: Full API integration tests
